# SentinelOps End-to-End (E2E) Test Suite

This directory contains opaque-box E2E integration tests verifying the full lifecycle of incidents, Bayesian memory updates, and safety policies.

## Execution
To run the full suite, execute:
```bash
./scripts/run_e2e.sh
```
